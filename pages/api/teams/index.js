// api endpoint: /api/teams
//TODO returns an array of objects of all teams + players in teams\

// import Data from "../../../data/players.json";

export default function handler(req, res) {

  res.status(200).json({ message: "WIP" });

}